export type TPlayer = {
    userName?: string,
    isLeader: boolean,
    userGiud: string,
    playerIsReady: boolean
}

export type TResPlayer = {
    user_name?: string,
    player_ready?: boolean,
    user_GUID: string,
    is_leader: boolean
}